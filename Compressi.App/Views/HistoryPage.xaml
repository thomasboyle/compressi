<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Compressi_App.Views.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Compressi_App.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Compressi.Core.Models"
    mc:Ignorable="d">

    <Grid Padding="24">
        <controls:GlassPanel>
            <controls:GlassPanel.PanelContent>
                <Grid RowSpacing="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Style="{StaticResource TitleTextStyle}" Text="History" />

                    <InfoBar
                        x:Name="HistoryInfoBar"
                        Grid.Row="1"
                        IsClosable="True"
                        IsOpen="False" />

                    <TextBox
                        x:Name="SearchBox"
                        Grid.Row="2"
                        PlaceholderText="Search by filename"
                        TextChanged="SearchBox_TextChanged" />

                    <Grid Grid.Row="3">
                        <ScrollViewer
                            x:Name="HistoryScrollViewer"
                            VerticalScrollBarVisibility="Auto">
                            <ItemsRepeater x:Name="HistoryList">
                                <ItemsRepeater.Layout>
                                    <StackLayout Spacing="4" />
                                </ItemsRepeater.Layout>
                                <ItemsRepeater.ItemTemplate>
                                    <DataTemplate x:DataType="models:HistoryEntry">
                                        <Grid Padding="8" ColumnSpacing="12">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Spacing="4">
                                                <TextBlock
                                                    FontFamily="{StaticResource AppPixelFontBoldFamily}"
                                                    Style="{StaticResource BodyTextStyle}"
                                                    Text="{x:Bind SourceName, Mode=OneTime}" />
                                                <TextBlock
                                                    Style="{StaticResource CaptionTextStyle}"
                                                    Text="{x:Bind CreatedAtDisplay, Mode=OneTime}" />
                                                <TextBlock
                                                    Style="{StaticResource CaptionTextStyle}"
                                                    Text="{x:Bind StatusDisplay, Mode=OneTime}" />
                                                <TextBlock
                                                    Style="{StaticResource CaptionTextStyle}"
                                                    Text="{x:Bind SizeSummaryDisplay, Mode=OneTime}" />
                                            </StackPanel>
                                            <StackPanel
                                                Grid.Column="1"
                                                Orientation="Horizontal"
                                                Spacing="8"
                                                VerticalAlignment="Center">
                                                <Button
                                                    Click="OpenFolderButton_Click"
                                                    Content="Folder"
                                                    Style="{StaticResource GlassSecondaryButtonStyle}"
                                                    Tag="{x:Bind}" />
                                                <Button
                                                    Click="RerunButton_Click"
                                                    Content="Re-run"
                                                    Style="{StaticResource GlassSecondaryButtonStyle}"
                                                    Tag="{x:Bind}" />
                                                <Button
                                                    Click="DeleteButton_Click"
                                                    Content="Delete"
                                                    Style="{StaticResource GlassDangerButtonStyle}"
                                                    Tag="{x:Bind}" />
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsRepeater.ItemTemplate>
                            </ItemsRepeater>
                        </ScrollViewer>

                        <StackPanel
                            x:Name="EmptyPanel"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Spacing="12"
                            Visibility="Collapsed">
                            <TextBlock
                                HorizontalAlignment="Center"
                                Foreground="{ThemeResource AppMutedInkBrush}"
                                Style="{StaticResource BodyTextStyle}"
                                Text="No compression history yet."
                                TextAlignment="Center" />
                            <TextBlock
                                HorizontalAlignment="Center"
                                MaxWidth="360"
                                Opacity="0.55"
                                Style="{StaticResource CaptionTextStyle}"
                                Text="Finished compressions appear here so you can open the output folder or re-run with the same settings."
                                TextAlignment="Center"
                                TextWrapping="WrapWholeWords" />
                            <Button
                                HorizontalAlignment="Center"
                                Click="GoToCompressButton_Click"
                                Content="Compress a video"
                                Style="{StaticResource GlassPrimaryButtonStyle}" />
                        </StackPanel>
                    </Grid>
                </Grid>
            </controls:GlassPanel.PanelContent>
        </controls:GlassPanel>
    </Grid>
</Page>
